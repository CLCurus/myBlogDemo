'use strict';

//这个配置文件就是用来配置这个程序的基本信息

module.exports = {
    viewCount : 2,
    connectionLimit : 10,
    host            : '127.0.0.1',
    user            : 'root',
    password        : 'root',
    database        : 'itcast',
}